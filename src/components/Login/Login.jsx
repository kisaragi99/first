import React from "react";
import s from "./Login.module.css"

const Login = (props) => {
    return (
        <div className={s.wrapper}>
            <h1>Please login</h1>
        </div>)
}

export default Login;